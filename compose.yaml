services:
  postgres:
    image: postgres:17-alpine
    environment:
      POSTGRES_DB: rideflow
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres
    ports:
      - "5432:5432"
    volumes:
      - rideflow-postgres:/var/lib/postgresql/data

volumes:
  rideflow-postgres:
